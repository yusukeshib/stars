[package]
name = "stars-native"
version = "0.1.0"
edition = "2021"

[dependencies]
stars-core = { path = "../../crates/stars-core" }
stars-catalog = { path = "../../crates/stars-catalog", features = ["filesystem"] }
wgpu = { workspace = true }
winit = "0.30"
pollster = "0.4"
env_logger = "0.11"
log = { workspace = true }
glam = { workspace = true }
